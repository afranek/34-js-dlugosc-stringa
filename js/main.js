let JS = "Uwielbiam JavaScript";
let Pro = "Jestem świetnym programistą";

function stringLength(x,y) {
    if (x.length > y.length) {
        console.log(x);
        } else{
        console.log(y);
    }
}
stringLength(JS,Pro);